{
  "name": "cv-management-system",
  "version": "1.0.0",
  "scripts": {
    "setup": "npm --prefix server run setup",
    "render:build": "npm install --prefix server && npm install --prefix client && npm --prefix server run db:generate && npm --prefix server run db:deploy && npm --prefix server run seed && npm --prefix client run build",
    "render:start": "npm --prefix server start",
    "start": "node scripts/start.mjs",
    "dev": "node scripts/dev.mjs",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "test": "npm --prefix server test && npm --prefix client test && npm --prefix client run lint && npm --prefix client run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Full-stack CV management course project"
}
